{% extends 'base.html.twig' %}
{% block body -%}
    <div class="container-fluid main">
        <div class="row">
            <div class="col">
                <div class="page-header">
                    <h1>Cycle edit</h1>
                </div>
                {{ form(edit_form) }}
                <ul class="record_actions">
                    <li>
                        <a href="{{ path('admin_cycle') }}">Back to the list</a>
                    </li>
                    <li>{{ form(delete_form) }}</li>
                </ul>
            </div>
        </div>

    </div>
{% endblock %}
