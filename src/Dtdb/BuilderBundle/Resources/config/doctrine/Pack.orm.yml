Dtdb\BuilderBundle\Entity\Pack:
    type: entity
    table: pack
    indexes:
        released_index:
            columns: [ released ]
        code_index:
            columns: [ code ]
        number_index:
            columns: [ number ]
    manyToOne:
        cycle:
            targetEntity: Cycle
            inversedBy: packs
            joinColumn:
                name: cycle_id
                referencedColumnName: id
    oneToMany:
        cards:
            orderBy: { 'number': 'ASC' }
            targetEntity: Card
            mappedBy: pack
        decklists:
            targetEntity: Dtdb\BuilderBundle\Entity\Decklist
            mappedBy: lastPack
    fields:
        id:
            type: integer
            id: true
            generator:
                strategy: AUTO
        code:
            type: string
            length: '10'
        name:
            type: string
            length: 255
        released:
            type: date
            nullable: true
        size:
            type: smallint
        number:
            type: smallint
    lifecycleCallbacks: {  }
